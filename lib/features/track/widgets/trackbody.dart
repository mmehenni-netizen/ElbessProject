import 'package:flutter/material.dart';

class Trackbody extends StatelessWidget {
  const Trackbody({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Track Body"),),
    );
  }
}