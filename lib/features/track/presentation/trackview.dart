import 'package:elbess/features/track/widgets/trackbody.dart';
import 'package:flutter/material.dart';

class Trackview extends StatelessWidget {
  const Trackview({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:  Trackbody(),
    );
  }
}