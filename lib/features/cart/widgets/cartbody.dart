import 'package:flutter/material.dart';

class Cartbody extends StatelessWidget {
  const Cartbody({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Cart Body"),),
    );
  }
}