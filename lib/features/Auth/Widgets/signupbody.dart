import 'package:flutter/material.dart';

class Signupbody extends StatelessWidget {
  const Signupbody({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Sign up with password")),
    );
  }
}